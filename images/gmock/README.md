# gmock

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#gmock

# Load into Docker
nix build .#load-gmock-to-docker && ./result/bin/load-gmock-to-docker
```
