# crossplane-provider-aws-dynamodb-fips

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-aws-dynamodb-fips

# Load into Docker
nix build .#load-crossplane-provider-aws-dynamodb-fips-to-docker && ./result/bin/load-crossplane-provider-aws-dynamodb-fips-to-docker
```
