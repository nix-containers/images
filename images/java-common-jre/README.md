# java-common-jre

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#java-common-jre

# Load into Docker
nix build .#load-java-common-jre-to-docker && ./result/bin/load-java-common-jre-to-docker
```
