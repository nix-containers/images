# docker-selenium-node-base

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#docker-selenium-node-base

# Load into Docker
nix build .#load-docker-selenium-node-base-to-docker && ./result/bin/load-docker-selenium-node-base-to-docker
```
