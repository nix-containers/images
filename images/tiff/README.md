# tiff

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#tiff

# Load into Docker
nix build .#load-tiff-to-docker && ./result/bin/load-tiff-to-docker
```
