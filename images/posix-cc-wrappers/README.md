# posix-cc-wrappers

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#posix-cc-wrappers

# Load into Docker
nix build .#load-posix-cc-wrappers-to-docker && ./result/bin/load-posix-cc-wrappers-to-docker
```
