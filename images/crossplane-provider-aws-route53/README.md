# crossplane-provider-aws-route53

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-aws-route53

# Load into Docker
nix build .#load-crossplane-provider-aws-route53-to-docker && ./result/bin/load-crossplane-provider-aws-route53-to-docker
```
