# docker-selenium-session-queue

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#docker-selenium-session-queue

# Load into Docker
nix build .#load-docker-selenium-session-queue-to-docker && ./result/bin/load-docker-selenium-session-queue-to-docker
```
