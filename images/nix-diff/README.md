# nix-diff

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#nix-diff

# Load into Docker
nix build .#load-nix-diff-to-docker && ./result/bin/load-nix-diff-to-docker
```
