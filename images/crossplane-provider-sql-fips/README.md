# crossplane-provider-sql-fips

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-sql-fips

# Load into Docker
nix build .#load-crossplane-provider-sql-fips-to-docker && ./result/bin/load-crossplane-provider-sql-fips-to-docker
```
