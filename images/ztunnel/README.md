# ztunnel

The ztunnel component of ambient mesh

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#ztunnel

# Load into Docker
nix build .#load-ztunnel-to-docker && ./result/bin/load-ztunnel-to-docker
```
