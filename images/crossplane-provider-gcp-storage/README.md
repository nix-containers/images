# crossplane-provider-gcp-storage

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-gcp-storage

# Load into Docker
nix build .#load-crossplane-provider-gcp-storage-to-docker && ./result/bin/load-crossplane-provider-gcp-storage-to-docker
```
