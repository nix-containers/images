# nnn

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#nnn

# Load into Docker
nix build .#load-nnn-to-docker && ./result/bin/load-nnn-to-docker
```
