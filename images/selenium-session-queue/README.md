# selenium-session-queue

Chainguard Containers are regularly-updated, secure-by-default container images

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#selenium-session-queue

# Load into Docker
nix build .#load-selenium-session-queue-to-docker && ./result/bin/load-selenium-session-queue-to-docker
```
