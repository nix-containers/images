# mesa-libgallium

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#mesa-libgallium

# Load into Docker
nix build .#load-mesa-libgallium-to-docker && ./result/bin/load-mesa-libgallium-to-docker
```
