# crossplane-provider-aws-kinesis

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-aws-kinesis

# Load into Docker
nix build .#load-crossplane-provider-aws-kinesis-to-docker && ./result/bin/load-crossplane-provider-aws-kinesis-to-docker
```
