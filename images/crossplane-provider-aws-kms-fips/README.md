# crossplane-provider-aws-kms-fips

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-aws-kms-fips

# Load into Docker
nix build .#load-crossplane-provider-aws-kms-fips-to-docker && ./result/bin/load-crossplane-provider-aws-kms-fips-to-docker
```
