# atmoz-sftp

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#atmoz-sftp

# Load into Docker
nix build .#load-atmoz-sftp-to-docker && ./result/bin/load-atmoz-sftp-to-docker
```
