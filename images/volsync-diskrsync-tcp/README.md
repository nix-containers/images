# volsync-diskrsync-tcp

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#volsync-diskrsync-tcp

# Load into Docker
nix build .#load-volsync-diskrsync-tcp-to-docker && ./result/bin/load-volsync-diskrsync-tcp-to-docker
```
