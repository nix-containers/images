# crossplane-provider-aws-lambda

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-aws-lambda

# Load into Docker
nix build .#load-crossplane-provider-aws-lambda-to-docker && ./result/bin/load-crossplane-provider-aws-lambda-to-docker
```
