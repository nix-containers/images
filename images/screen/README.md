# screen

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#screen

# Load into Docker
nix build .#load-screen-to-docker && ./result/bin/load-screen-to-docker
```
