# crossplane-provider-gcp-kms

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-gcp-kms

# Load into Docker
nix build .#load-crossplane-provider-gcp-kms-to-docker && ./result/bin/load-crossplane-provider-gcp-kms-to-docker
```
