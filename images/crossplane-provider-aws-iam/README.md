# crossplane-provider-aws-iam

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-aws-iam

# Load into Docker
nix build .#load-crossplane-provider-aws-iam-to-docker && ./result/bin/load-crossplane-provider-aws-iam-to-docker
```
