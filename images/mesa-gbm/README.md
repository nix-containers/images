# mesa-gbm

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#mesa-gbm

# Load into Docker
nix build .#load-mesa-gbm-to-docker && ./result/bin/load-mesa-gbm-to-docker
```
