# katib-suggestion-hyperopt

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#katib-suggestion-hyperopt

# Load into Docker
nix build .#load-katib-suggestion-hyperopt-to-docker && ./result/bin/load-katib-suggestion-hyperopt-to-docker
```
