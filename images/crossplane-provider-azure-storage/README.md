# crossplane-provider-azure-storage

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-azure-storage

# Load into Docker
nix build .#load-crossplane-provider-azure-storage-to-docker && ./result/bin/load-crossplane-provider-azure-storage-to-docker
```
