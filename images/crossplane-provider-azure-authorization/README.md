# crossplane-provider-azure-authorization

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-azure-authorization

# Load into Docker
nix build .#load-crossplane-provider-azure-authorization-to-docker && ./result/bin/load-crossplane-provider-azure-authorization-to-docker
```
