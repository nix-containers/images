# crossplane-provider-aws-firehose-fips

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-aws-firehose-fips

# Load into Docker
nix build .#load-crossplane-provider-aws-firehose-fips-to-docker && ./result/bin/load-crossplane-provider-aws-firehose-fips-to-docker
```
