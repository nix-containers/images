# nix-tree

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#nix-tree

# Load into Docker
nix build .#load-nix-tree-to-docker && ./result/bin/load-nix-tree-to-docker
```
