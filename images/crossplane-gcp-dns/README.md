# crossplane-gcp-dns

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-gcp-dns

# Load into Docker
nix build .#load-crossplane-gcp-dns-to-docker && ./result/bin/load-crossplane-gcp-dns-to-docker
```
