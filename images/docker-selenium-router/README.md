# docker-selenium-router

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#docker-selenium-router

# Load into Docker
nix build .#load-docker-selenium-router-to-docker && ./result/bin/load-docker-selenium-router-to-docker
```
