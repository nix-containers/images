# dbus-x11

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#dbus-x11

# Load into Docker
nix build .#load-dbus-x11-to-docker && ./result/bin/load-dbus-x11-to-docker
```
