# crossplane-provider-aws-rds

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-aws-rds

# Load into Docker
nix build .#load-crossplane-provider-aws-rds-to-docker && ./result/bin/load-crossplane-provider-aws-rds-to-docker
```
