# crossplane-provider-gcp-pubsub

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-gcp-pubsub

# Load into Docker
nix build .#load-crossplane-provider-gcp-pubsub-to-docker && ./result/bin/load-crossplane-provider-gcp-pubsub-to-docker
```
