# selenium-server

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#selenium-server

# Load into Docker
nix build .#load-selenium-server-to-docker && ./result/bin/load-selenium-server-to-docker
```
