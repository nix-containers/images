# crossplane-provider-gcp-compute

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-gcp-compute

# Load into Docker
nix build .#load-crossplane-provider-gcp-compute-to-docker && ./result/bin/load-crossplane-provider-gcp-compute-to-docker
```
