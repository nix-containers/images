# crossplane-provider-aws-iam-fips

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-aws-iam-fips

# Load into Docker
nix build .#load-crossplane-provider-aws-iam-fips-to-docker && ./result/bin/load-crossplane-provider-aws-iam-fips-to-docker
```
