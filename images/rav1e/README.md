# rav1e

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#rav1e

# Load into Docker
nix build .#load-rav1e-to-docker && ./result/bin/load-rav1e-to-docker
```
