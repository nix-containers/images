# docker-selenium-base

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#docker-selenium-base

# Load into Docker
nix build .#load-docker-selenium-base-to-docker && ./result/bin/load-docker-selenium-base-to-docker
```
