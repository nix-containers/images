# crossplane-provider-aws-sqs

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-aws-sqs

# Load into Docker
nix build .#load-crossplane-provider-aws-sqs-to-docker && ./result/bin/load-crossplane-provider-aws-sqs-to-docker
```
