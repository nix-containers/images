# crossplane-provider-aws-cloudwatchlogs-fips

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-aws-cloudwatchlogs-fips

# Load into Docker
nix build .#load-crossplane-provider-aws-cloudwatchlogs-fips-to-docker && ./result/bin/load-crossplane-provider-aws-cloudwatchlogs-fips-to-docker
```
