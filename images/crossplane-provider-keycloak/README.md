# crossplane-provider-keycloak

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-keycloak

# Load into Docker
nix build .#load-crossplane-provider-keycloak-to-docker && ./result/bin/load-crossplane-provider-keycloak-to-docker
```
