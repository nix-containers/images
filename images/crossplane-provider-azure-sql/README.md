# crossplane-provider-azure-sql

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-azure-sql

# Load into Docker
nix build .#load-crossplane-provider-azure-sql-to-docker && ./result/bin/load-crossplane-provider-azure-sql-to-docker
```
