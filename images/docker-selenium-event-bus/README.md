# docker-selenium-event-bus

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#docker-selenium-event-bus

# Load into Docker
nix build .#load-docker-selenium-event-bus-to-docker && ./result/bin/load-docker-selenium-event-bus-to-docker
```
