# just-newuidmap-newgidmap

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#just-newuidmap-newgidmap

# Load into Docker
nix build .#load-just-newuidmap-newgidmap-to-docker && ./result/bin/load-just-newuidmap-newgidmap-to-docker
```
