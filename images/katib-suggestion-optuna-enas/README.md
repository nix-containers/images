# katib-suggestion-optuna-enas

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#katib-suggestion-optuna-enas

# Load into Docker
nix build .#load-katib-suggestion-optuna-enas-to-docker && ./result/bin/load-katib-suggestion-optuna-enas-to-docker
```
