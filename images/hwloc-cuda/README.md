# hwloc-cuda

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#hwloc-cuda

# Load into Docker
nix build .#load-hwloc-cuda-to-docker && ./result/bin/load-hwloc-cuda-to-docker
```
