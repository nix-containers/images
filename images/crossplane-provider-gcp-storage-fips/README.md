# crossplane-provider-gcp-storage-fips

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-gcp-storage-fips

# Load into Docker
nix build .#load-crossplane-provider-gcp-storage-fips-to-docker && ./result/bin/load-crossplane-provider-gcp-storage-fips-to-docker
```
