# lazygit

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#lazygit

# Load into Docker
nix build .#load-lazygit-to-docker && ./result/bin/load-lazygit-to-docker
```
