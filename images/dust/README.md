# dust

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#dust

# Load into Docker
nix build .#load-dust-to-docker && ./result/bin/load-dust-to-docker
```
