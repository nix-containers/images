# crossplane-provider-family-aws

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-family-aws

# Load into Docker
nix build .#load-crossplane-provider-family-aws-to-docker && ./result/bin/load-crossplane-provider-family-aws-to-docker
```
