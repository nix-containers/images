# wavefront-proxy-licenses

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#wavefront-proxy-licenses

# Load into Docker
nix build .#load-wavefront-proxy-licenses-to-docker && ./result/bin/load-wavefront-proxy-licenses-to-docker
```
