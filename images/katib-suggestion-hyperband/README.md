# katib-suggestion-hyperband

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#katib-suggestion-hyperband

# Load into Docker
nix build .#load-katib-suggestion-hyperband-to-docker && ./result/bin/load-katib-suggestion-hyperband-to-docker
```
