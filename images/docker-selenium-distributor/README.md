# docker-selenium-distributor

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#docker-selenium-distributor

# Load into Docker
nix build .#load-docker-selenium-distributor-to-docker && ./result/bin/load-docker-selenium-distributor-to-docker
```
