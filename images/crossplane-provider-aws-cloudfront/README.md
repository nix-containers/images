# crossplane-provider-aws-cloudfront

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-aws-cloudfront

# Load into Docker
nix build .#load-crossplane-provider-aws-cloudfront-to-docker && ./result/bin/load-crossplane-provider-aws-cloudfront-to-docker
```
