# librechat

An open-source AI chat application

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#librechat

# Load into Docker
nix build .#load-librechat-to-docker && ./result/bin/load-librechat-to-docker
```
