# docker-selenium-hub

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#docker-selenium-hub

# Load into Docker
nix build .#load-docker-selenium-hub-to-docker && ./result/bin/load-docker-selenium-hub-to-docker
```
