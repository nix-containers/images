# gnupg-gpgconf

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#gnupg-gpgconf

# Load into Docker
nix build .#load-gnupg-gpgconf-to-docker && ./result/bin/load-gnupg-gpgconf-to-docker
```
