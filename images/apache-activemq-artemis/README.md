# apache-activemq-artemis

Last changed 23 hours ago

Request a free trial

Contact our team to test out this image for free.

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#apache-activemq-artemis

# Load into Docker
nix build .#load-apache-activemq-artemis-to-docker && ./result/bin/load-apache-activemq-artemis-to-docker
```
