# docker-selenium-standalone

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#docker-selenium-standalone

# Load into Docker
nix build .#load-docker-selenium-standalone-to-docker && ./result/bin/load-docker-selenium-standalone-to-docker
```
