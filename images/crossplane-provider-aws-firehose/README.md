# crossplane-provider-aws-firehose

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#crossplane-provider-aws-firehose

# Load into Docker
nix build .#load-crossplane-provider-aws-firehose-to-docker && ./result/bin/load-crossplane-provider-aws-firehose-to-docker
```
