# docker-selenium-sessions

## Build Information

| Metric | Value |
|--------|-------|
| Build Time | N/A |
| Build Status | unknown |

## Usage

```bash
# Build the image
nix build .#docker-selenium-sessions

# Load into Docker
nix build .#load-docker-selenium-sessions-to-docker && ./result/bin/load-docker-selenium-sessions-to-docker
```
